---
topic: Aurora architecture spine v1.7
scope: project (initiative altitude)
purpose: build-substrate
altitude: initiative
updated: 2026-09-22T00:29
---

- (event) Input: ADR doc 'Aurora_Architecture_Decisions_v1_7_final.docx' (Sections 1–26, v1.6 superseded, v1.7 current frozen reference). Intent: create. Mode: Fast path requested by user.
- (assumption) Fast path requested by user: draft spine fast with [ASSUMPTION] tags, review/correct in review
- (decision) Paradigm: composite frozen in v1.7 = Modular Monolith + Vertical Slices + Hexagonal (Ports & Adapters) + Local-First + targeted Event-Driven + central Agent Kernel. [ADOPTED]
- (decision) AD: no microservices in V1 — modular boundaries in code only; extraction only on measurable need. [ADOPTED]
- (decision) AD: platform strategy — Phase 1 mobile-only (Ionic React + Capacitor, Android); Electron desktop Phase 2 after mobile production stable. 'Mobile Only for delivery, Platform-Agnostic for the core.' [ADOPTED]
- (decision) AD: domain/application never depend on a vendor, framework, or concrete model name; all external providers behind typed Ports/Providers; optional capabilities must degrade gracefully when absent.
- (decision) AD: module boundaries — a business module never touches another module's internal tables; explicit contracts only. Vertical slices own their scope, tests and contracts.
- (decision) AD: no model key or secret in mobile or web — all AI calls go through server-side Gateway/Router; mobile sees a normalized AIProvider contract only.
- (decision) AD: multi-provider AI: Agent Kernel → Context Builder → Task Classifier → AI Router → AI Policy/Budget → Cloudflare AI Gateway → Provider Adapter. Agnes primary; Workers AI second pool; Groq/Cerebras optional; fallback = dedicated Cloudflare Worker calling Workers AI directly (no business logic in the Worker). [ADOPTED]
- (decision) AD: fallback rules — retry on transient failure, fallback on unavailability/incompatibility/limit; a 429 is never bypassed by rotating keys/accounts; fallback responses stay traceable (provider, model, attempt, reason); free tiers are opportunistic, not SLA, and the registry can auto-retire them.
- (decision) AD: data — PostgreSQL transactional source of truth + pgvector for semantic retrieval; Semantic Tree = knowledge hierarchy (truth lives in Knowledge Base, never in the rendering engine); Event History kept for progression/audit without full Event Sourcing (excluded V1).
- (decision) AD: local-first — PowerSync + SQLite on device, sync to Supabase; the UI always reads local state first.
- (decision) AD: async jobs — everything long/heavy (OCR, transcription, artifact generation, search) is a persisted, idempotent, retryable, observable Job; Supabase Cron → Aurora dispatcher → persisted jobs → Edge Functions/Workers. Never blocks the UI.
- (decision) AD: events are targeted decoupling only — a simple transactional operation stays a direct command; named event list (TaskCompleted, CourseImported, FlashcardReviewed, ProgressEvidenceCreated, SkillStateChanged, GoalUpdated, ArtifactGenerated, JobCompleted, DiscoveryItemCreated).
- (decision) AD: visualization stack frozen — @xyflow/react + @dagrejs/dagre (Semantic Tree), @antv/infographic (explanations), @antv/g2 (data), KaTeX (LaTeX), motion (animation); all behind internal renderer contracts (SemanticTreeRenderer, InfographicRenderer, DataVisualizationRenderer, MathRenderer, AnimationController) so domain never imports engine details. [ADOPTED]
- (assumption) [ASSUMPTION] pnpm monorepo with package layout packages/ui, packages/data, packages/agent, packages/scientific-engine, packages/integrations, apps/mobile (Ionic) — inferred from ownership matrix, exact tree to be ratified in wave 0.
- (decision) AD: knowledge integrity — fidelity to source corpus: corpus definitions/formulas stay authoritative; agent explanations are explicitly separated and labeled; every significant concept carries provenance (document, page, passage). Applies to review sheets, infographics, semantic tree.
- (decision) AD: agent coherence — one central Agent Kernel (not separately deployed agents); Planner/Coach/Tutor/Researcher/Executor are kernel capabilities coordinated through the loop Intent → Context → Plan → Retrieve → Tools → Verify → Action → Result → Memory. Context forms: Intent, Personal, Productivity, Learning, Discovery, Semantic, Expert Skills, Tool, Permission.
- (decision) AD: contract packs for parallel agent development — each domain ships a Contract Pack (responsibilities, owned files, public TS interfaces, events, allowed deps, DoD, mandatory tests); two agents never modify the same central files simultaneously; main always buildable; merges via PR + Codex review. [ADOPTED]
- (assumption) [ASSUMPTION] Supabase (PostgreSQL/Auth/Edge Functions) + Cloudflare (R2, Workers, AI Gateway) is the infra envelope; environments (dev/staging/prod) and RLS policy details to be fixed in wave 0, not by this spine.
- (event) Spine drafted (14 ADs + conventions + stack + structural seed + capability map + deferred + open questions). Deterministic lint: 0 findings. Adversarial reviewer subagent dispatched (reviewer-gate), result pending — will integrate findings and distill fixes.
- (event) Reviewer gate: adversarial review returned 10 findings (2 Critical, 2 High, 3 Medium, 3 Low). All integrated into spine: AD-6/AD-7/AD-9/AD-12/AD-13 tightened; AD-15 (SSoT types) + AD-16 (operational envelope) added. Lint re-run: 0 findings.
- (event) Spine finalized: status=final, 16 ADs. Sources: docx v1.7 + adversarial review.
- (event) SPEC.md assembled - 5 dimension packs + design system, 0 findings
- (decision) Thème : LIGHT par défaut (thème de référence 'Technical Calm'). Dark mode = thème secondaire à part entière avec sa propre logique (pas un renversement), accessible via toggle dans Settings (Light [défaut] / Dark). Appliqué via les tokens, composants identiques en code.
- (decision) Système de thème multi-couches (contrat AD-17 candidate) : 3 couches = (1) primitives figées (typo/spacing/radius/durées), (2) 2 styles NEUTRES figés (Aurora Light #FFFFFF / Aurora Dark #121212), (3) 5 thèmes d'ACCENT extensibles (Aurora défaut #0066CC, Océan #0077B6, Forêt #2E5E3A, Terre #B85C38, Monochrome #4A4A5A). Résolution 3 niveaux token→valeur. Le code ne lit que les tokens sémantiques → identique quel que soit le thème. Les modules/features peuvent adapter certains tokens au niveau local ou écran (déclaratif). Les thèmes vivent dans packages/ui (AD-15).
- (decision) REVISION theme system : l'utilisatrice veut un produit ESTHETIQUE, VIVANT, COLORE, FLUIDE et CLAIR. Le catalogue de 5 accents neutres est remplace par 10 THEMES VIVANTS (Aurora, Boreal, Sahara, Vesper, Verve, Ember, Lagoon, Prism, Slate, Halo), chacun avec nom + description + logique de couleur motivee + 6 tokens concrets + usage recommande. Le style neutre light/dark (couche 1) reste, les themes vivants (couche 2) se superposent. Le code ne lit que les tokens semantiques.
- (decision) Correction style neutre light : le canvas n'est PAS du blanc pur #FFFFFF (agressif pour les longues sessions). Off-white #F8F9FA (type Canva clair) en fond global ; le blanc pur #FFFFFF n'apparaît que sur les surfaces surélevées (cartes/panneaux) pour la profondeur. Dark : canvas #121212 (pas de noir pur), surfaces surélevées plus claires.
- (decision) THEME SYSTEM REFAIT : (1) REGLE 1 : un theme ne definit JAMAIS la semantique fonctionnelle - success/warning/danger/info restent des tokens sémantiques indépendants, avec adaptations light/dark. En thème Sakura une tache urgente ne devient pas rose. (2) Nouveau catalogue 10 thèmes (univers) : Aurora (dawn/tech), Lagoon (eau), Boreal (nord), Sakura (cerisier - HUMAN), Vesper (crépuscule), Solara (or/ambition, remplace Halo), Terra (terre cuite, ex-Sahara), Verdant (végétation, ex-Verve), Citrus (agrumes/ludique), Cosmos (espace/nébuleuse, ex-Prism - règle 1 dom + 1 sec + 1 accent). (3) Retraits : Ember (collision danger), Slate (reclassé preset), Nocturne + High Contrast = presets spécialisés hors des 10. (4) Architecture 3 niveaux : Style Neutre (Light #F8F9FA / Dark #121212) x Thème Expressif (10) x Adaptation Locale (module/écran) - JAMAIS changer de thème par module, seulement adaptation déclarative. (5) 1 thème = 10 dimensions (colors, gradients, illustrationMood, iconTreatment, selectionTreatment, focusTreatment, chartPalette, decorativeShapes, motionMood) - pas juste 5 variables CSS. (6) LIVRABLE FINAL : test des 10 thèmes sur 5 écrans réels (accueil, focus, arbre, progress, discovery) - aperçus ASCII/mockups + verdicts. Le test prouve que les thèmes sont 'vraiment beaux', pas juste sur papier.
- (event) Pack 05 reconstitué : §4 propre (rejointure de 05-part2) + §5 v2 (système 10 thèmes vivants + 3 presets + règle thème/sémantique + AD-17 candidate) intégré. 8 débris (6 chunks + part2 + fix-tail) supprimés. SPEC.md mis à jour (OQ-14/15/16 + plan wave 0 packages/ui/src/themes/).
- (decision) REVUE INTER-PACKS integree (reviews/review-pack-coherence.md) : 0 Critical, 1 High, 5 Medium, 4 Low. Corrections appliquees dans les packs : (1) H1 - SSoT FocusSessionBilan fige dans 05 §3.6.9 (shape packages/domain + ChartSpec focusBilan + composants DS) ; (2) 02 §3.3 UiState.theme → enum AuroraTheme + themeStyle orthogonal ; (3) 02 §5.2 InfographicRendererProps.theme → AuroraTheme ; (4) 02 §8 theming AD-17 v2 + migration binaire→enum par packages/domain ; (5) 04 O3b - theme par défaut Android = persisté (pas prefers-color-scheme) ; (6) 01 §4 Identity user_context porte l'enum v2, migrateur en packages/domain.
- (event) Pack 05 v2 finalise : §4 complet (8 écrans 4.5.2-4.9.2 rewrite par agent pack05-fix, SSoT FocusSessionBilan AD-15 §3.6.9), §5 système multi-couches AD-17 candidate (10 thèmes vivants + 3 presets + regle theme/semantique + resolution 3 niveaux), §6/§7 consolidés. Débris 05-main.md/05-p2.md supprimés. Correctifs inter-packs : 02 UiState.theme=AuroraTheme+themeStyle, 02 §8 Dark mode → v2, 04 O3b theme persisté Android, 01 §4 Identity user_context=enum v2. Review pack-coherence : 0 Critical / 1 High / 5 Medium / 4 Low — H1 traité (FocusSessionBilan SSoT), 5 mustFixForV2 appliqués.
- (event) Pack 05 v2 finalise et pousee : 8 écrans casses (4.5.2–4.9.2) reecrits proprement via agent pack05-fix, débris (05-part2.md, 05-chunk*.txt, 05-fix-tail.md) nettoyés, SPEC.md mis a jour (OQ-14/15/16, plan wave 0 packages/ui/src/themes/), reves inter-packs integrees (H1 FocusSessionBilan SSoT + 5 mustFixForV2 dans 01/02/04). Lint du spine : 0 findings.
- (event) Pack 05 finalise : renumerotation des references internes (le texte broye 05-main avait un ancien systeme sequentiel 4.1-4.40 ; tout est remappe sur le systeme actuel 4.x.y par module) + ecriture propre des 8 ecrans 4.5.2-4.9.2. 0 ref cassee. Lint : 0 finding.
- (decision) TRANCHES OQ-14/15/16 + AD-17 fige au spine : OQ-14 = 10 themes V1 (mcanisme + catalogue) + 3 presets ; OQ-15 = adaptations locales V1 = Focus Mode, Lecture/Scientific = V1.1 (port LocalThemeOverride prevu ds V1) ; OQ-16 = Nocturne = preset autonome (JSON dedi, canvas #0A0E1A) ; AD-17 Multi-theme skin system with invariant semantic states [ADOPTED] ajoute au spine (17 ADs total) - 3 niveaux : style neutre (light #F8F9FA / dark #121212, tokens smantiques success/warning/danger/info) x theme expressif (10 vivants) x adaptation locale (Focus Mode V1) ; user_context.theme = enum AuroraTheme SSoT packages/domain.
